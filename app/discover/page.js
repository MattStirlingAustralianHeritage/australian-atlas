import DiscoverClient from './DiscoverClient'

export const metadata = {
  title: 'Discover — Australian Atlas',
  description: 'One place at a time. Explore Australia\'s independent places through serendipity.',
}

export default function DiscoverPage() {
  return <DiscoverClient />
}
