'use client'

// TODO: Enable when producer_picks table is created on the master portal.
// The producer_picks feature currently lives on individual verticals (SBA, Fine Grounds, Rest)
// with their own tables (producer_picks, roaster_picks, host_picks).
// This component will query the master portal's producer_picks table once it exists.

export default function ProducerPicks({ venueId, venueName, vertical }) {
  return null
}
